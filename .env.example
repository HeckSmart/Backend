# Server
PORT=3000
NODE_ENV=development

# MariaDB (Sequelize)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=letskillit
DB_POOL_MAX=5
DB_POOL_MIN=0
# Connection timeout in ms (default 10000). Increase if DB is slow or remote.
DB_CONNECT_TIMEOUT=10000
